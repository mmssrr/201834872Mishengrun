barnett bruce barnett subject title xterm article lanzo mark lanzo write hint sun user use instead csh built-in echo otherwise embed literal esc bel character string instead use convenient octal sequence bad idea use slower built-in echo doe execute hostname per shell window doe read one extra file manipluating title doe require execute extra program oh yes doe execute program per system store file next time hostname setenv hostname hostname endif figure generate escape bell echo command without line terminator may variable e set execute script system e echo already set echo variable else f echo hostname source echo hostname else echo n wc l echo build echo bsd berkeley style echo echo ech echo n echo hostname echo set e echo tr echo hostname echo set b echo tr echo hostname echo n echo hostname source echo hostname else echo build echo sysv echo ech echo echo hostname echo e echo hostname echo b echo hostname echo n c echo hostname source echo hostname endif use shelltool cmdtool xterm duplicate alias avoid problem term sun sun alias alias header ech e l e n alias iheader ech e l e n else term xterm alias header ech e b n alias iheader ech e b n endif bruce barnett barnett uunet barnett 