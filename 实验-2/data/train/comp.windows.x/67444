lindholm george lindholm subject xv ha escape article lindholm george lindholm write article bradley john bradley write another false alarm certainly next week message real thing repeat drill batten hatch hide woman lock cow xv ha finally escape wa clean cage morning overpower break office door flee lab wa last see head general direction nearly per second find answer name blast vacation capital waco texas first patch follow fix problem imakefile file make possible compile solaris without use ucb stuff oops get signal process right thats get work ascii terminal fix patch imakefile program xv bggen vdcomp xcmap define havejpeg define havetiff ifdef havejpeg program xv bggen vdcomp xcmap echo echo remember depend first echo define havejpeg define havetiff ifdef havejpeg subdirs tiff endif define passcdebugflags makesubdirs subdirs dependsubdirs dubdirs endif echo echo remember depend first echo echo build xv echo subdirs tiff endif define passcdebugflags define ihavesubdirs makesubdirs subdirs endif echo build xv echo srcs dependtarget alltarget program normalprogramtarget xv deplibs normalprogramtarget bggen deplibs srcs alltarget program dependtarget ifdef ihavesubdirs dependsubdirs subdirs endif normalprogramtarget xv deplibs normalprogramtarget bggen deplibs include file include define sun define sysv include else include endif include include signal macro define sun define sysv define sighold sigalrm define sigrelse sigalrm define sigpause sigalrm else define sigblock sigmask sigalrm define sigblock define sigpause endif default folk define random rand define srandom srand endif define sun define sysv define random rand define srandom x srand x endif imakefile hasgcc rm cc traditional cflags rm else rm cc cflags rm endif depend clean rm dependtarget 