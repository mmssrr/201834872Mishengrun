from subject re ide v scsi in wlsmith smith write stuff delete so the lowly low-density pc fdd use the hdd controller doe sense it doe of sense fix disk sector buffer by the controller them to memory with insw or the instruction be be quite efficient single instruction go the know how to use the bus since the the bus be likely resource to cpu it possible bus interference would much or more loss of cpu cycle for doe the insw sequence the floppy on the other be not buffer so use the cpu for floppy do on the pc jr by the doe stink 