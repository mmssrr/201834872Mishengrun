from dieter dieter subject re vl-bus controller or ide controller in beng write hi i buy mb be decide controller be vl-bus controller bite ide controller i the vl bus controller be slower ide controller which one be true the vl-ide be much then the ide it depend on the drive you use the you use i use vl-ide contr it be test under coretest with use use you figure out the jumper set on the controller to get set get under coretest but doe not work you to the bios to get the speed i use controller yet but it might speed up twice dieter dieter dieter kiel of the wom tel voice compuserve 