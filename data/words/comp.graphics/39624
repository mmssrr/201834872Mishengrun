from ttknock bos hogg subject pov in to do with pov i two little one be c will perform morph between two point give the of for the morph then it will write the point the function etc out to file then i perl script will the list of function insert them into pov file give line i hop this would let me do simple however i discover simply perform on object will not spin object but will the object the now i know this would be to first the object to the origin perform the then move it but i know there must be this i think it i object the origin then them to new point then do the which could this however this occur on object not help be ttknock 