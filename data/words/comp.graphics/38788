from subject inv doe truely the error give you while render it seem to present itself while render i circumvent this problem by render the line however it would be nice to render inside the editor be this memory problem 