from mikey mike subject re monthly question expose event in moschetti write post of the question just doe seem to get how we resist question something like this the problem of course be no expose event be if the window be visible we need to do be somehow tickle the window so the expose be hit with will it to render just the of the window the new item be the best to tickle window to produce this if your expose event be truly intelligent expose then you use to expose event your pixel be not none for the enclose of your new item this be still not since other item within will still be if your expose event simply everything you be do much more work just the new item on top of the exist scene mike silicon inc mikey 