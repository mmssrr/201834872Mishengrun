from subject re view postscript file under x window in kin kin cho write i live with utility convert postscript to text count so i know how the document well i know of one to sort of do this conversion first get ghostscript check out the file come with it it print out some where text string go on the count i write little c to the output of g so you get the string in the document no it wo up though i use it with degree of success try this out it do you filter for use with g doe crude postscript conversion g i print ctrl-l new put the follow source into compile it include include line p while fgets line line stdin null if line printf else if line line if p strrchr line null continue p if p strchr line null continue for p p p if p p p p return 