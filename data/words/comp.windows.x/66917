from gringort joel gringorten subject re cursor problem in mbell mike bell write for those who this before but no-one solve this one yet so i post sorry i problem cursor bigger i use motif run hp-ux the function return result but no cursor when the be bigger the i try use the follow code unsigned int w h xquerybestcursor xtwindow w h to return the to i require however it return the width height respectively i do wrong if not how i get round the problem doe the you use cursor support or doe the server cursor you tell the difference just by use it if the cursor blink lot when there screen it cursor if it cursor i think you bug in hp x server i not with cursor chip cursor it quite possible the server be just echo your prefer cursor without check it i very old mit server revision do just in you stick with cursor of the server return joel 