from melnick subject some question big brother clipper in previous post by bellovin it report the big brother clipper chip will encrypt in bite block be do with de the user key k which be derive from etc be use to encrypt m be then use to form three this three consist of e m k k encrypt with some other key the chip number this three be then encrypt by still key my question be be for we with encrypt voice the speech be then some number of group into bite block this block be then encrypt over non-secure link where be the three be something to bite block be send the begin of the session if the be send the begin of the session how do they insure the equipment use the chip doe just just product use the chip be require to 