from s subject clipper chip request the clipper on the net over the few lot of uninformed the chip it us government plot etc i the follow question doe the clipper chip implement just the or doe it implement chip to chip protocol if it doe implement protocol it be use just crypt chip where the chip spec sheet be who them under condition there restriction to how the chip be use in system the security of the the encrypt doe not to require the key be secret why be it secret when the key become well know if it secret to more difficult doe the enforcement how much how be it the chip be to the unit key the chip number of the s 