from denning subject the clipper chip the follow document the clipper chip how it be use how of the chip be couple to key the escrow process how enforcement decrypt since there be some on this news group my own involvement in this project i like to i not in involve i find out it when the fbi brief me on even since then i spend time with the fbi to more this i the nist brief the of commerce on the document below be the result of effort dorothy denning the clipper chip dorothy denning introduction on the president new will bring together the government industry in to provide secure while meet the need of enforcement the of the be new encryption chip the clipper chip together with split-key to escrowing key two escrow use the key from both need to reconstruct key chip structure the clipper chip block encryption the us bite key with for the de round of with for the de it support de mode of throughput be mbit second chip include the follow component the encryption f key be common to chip n number u secret key unlock encrypt with the chip encrypt with the chip to see how the chip be use it be embed in the t telephone security device it will be suppose i someone we both such device push button to secure my security device will session key k with the device the other end in method of key be use the key k m voice then feed into the clipper chip to produce two e m k the encrypt e e k u n f enforcement block the enforcement block thus the session key k encrypt under the unit key u with the number n encrypt under the key f chip escrow clipper chip inside scif secure computer which be the scif computer equipment to the chip chip during single session the scif be mikotronx the begin of session trust from of the two key escrow enter the enter into the enter these serve seed to key for sequence of number to the unit key for number n the n be first with fix block to produce block then use key to triple-encrypt produce block e d e n be with two other block to produce two block compute e d e e d e then together give bite the first bite to the second bite to the rest the unit key u be the xor of the key escrowed with the two escrow sequence of for u they write onto three floppy disk the first disk file for number the correspond key the second disk be but the the third disk the unit key u the first disk the second disk the third disk be use to the chip the chip be from the the the be destroy for no be leave behind the protocol be slightly so four people in the room of two the first two would provide the seed the second two the escrow would the disk to the escrow the escrow yet to be determine but they will not be the fbi or other enforcement one or both be independent from the government enforcement use when enforcement be to encrypt line they will first the to the service provider in order to get to the line let u the be in they determine the line be encrypt with clipper they will first decrypt the enforcement block with the key f this give them e k u n they will then identify the chip number n to of the key escrow get xored together to produce the unit key u e k u be decrypt to get the session key k the be decrypt this will be through box decoder by the fbi distribution notice be on provide by nist the fbi permission to distribute this document be 