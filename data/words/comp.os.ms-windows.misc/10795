from subject re segment memory model hello phjm you write firstly doe window in mode do with dlls be compile use the memory model no we be tell even in mode window will dlls into memory below it no my second question to inside dlls be there to get multiple of the segment dgroup no but you within code segment when you need more memory you on the you forget most of write memory under you memory you lock every block without the memory you use pointer everytime without lock unlock the memory block besides dll mostly just disguise exe to be by tel ci bix crosspoint 