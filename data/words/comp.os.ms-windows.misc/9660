from ismo k rkk inen subject re in window write be there utility will window select one of your window directory bmp file the file there couple i use which be if you need just to the then i write short in vb doe this by use the news be you need to run it the dll be some the be the order of picture depend on the system the number of bmp-files in the directory so the picture the if you execute the multiple time during the the number of bmps not if you feel this be you need then i could uuencode it it to you it be ftp ismo k rkk inen cthulhu 