from subject reply when i use the for window such or it doe use the whole window i when the window be it still scroll the of window it doe not use whole window i set stty row but still the scroll from the top of the window could tell me how to setup these to use whole window in 