from subject quicktime jpeg source need hello we need the code for decompression of quicktime jpeg pict file they do conform to jpeg compressor which in source the code should be in c or the code will be use on to view picture over isdn will be put it much le then only dsp with some support chip if the code be not the be to decompress them 