from subject need hello we look for complete with source code will run on our unix system sequent run dynix this must support wide of device pc run this point be not option this should if possible support these sort of minimum requirement log etc with precise multi-line etc minor on etc it would be nice if some limit of color plot be if the output device support it we copy of gnuplot currently use it but gnuplot some we look for something more robust gnuplot this point i look for might solve our problem if you me the if the product you know be send phone number or so i them price etc northwest 