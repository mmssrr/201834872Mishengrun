from bob subject re in bill bill write high-end come with c source code for do sort of thing color etc doe such exist for generic hopefully in the public this would be for the purpose of compile under non-dos system run on pc i interest in info both public to do pc i currently work on run on pcc with non-dos kernel need to do some simple i not sure if of the be go to be issue or not i suspect i implement the controller server process request queue on one time if this provide sufficiently frequent then i believe i restrict to be perform by the server thus to the to this single process the need for be new to the system world i be overlook something do you think cheer bob 