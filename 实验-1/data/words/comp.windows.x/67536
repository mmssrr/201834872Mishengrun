from morris morris hirsch subject none we motif to of these write loop with these step use semop to check if true new otherwise block new come quite often use msgop to check for if true them otherwise continue user not often the to we of these run in window plus producer serve some device set the everyone block sure the device server get to run xt xm loop model we must fit into vol iv ch discus how to workprocs run during idle time we our exist loop it workproc the problem be our block until new be no longer neither be let the free-run others hurt the unix select let you block until of we for the x loop except not file suggestion or most welcome we run sgi iris indigo system morris hirsch morris or 