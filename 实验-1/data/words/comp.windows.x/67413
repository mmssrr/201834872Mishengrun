from lindholm george lindholm subject re xv in john write no not not it be do by next week no this be the thing i this be not drill down the hide the woman lock up the cow xv i it this morning when it overpower me break down the office door flee the it see in the direction of per second if find it to the of i off to the of the here be the first the follow fix some problem with the file it possible to compile under without to use the ucb stuff xv bggen vdcomp if define define ifdef xv bggen vdcomp echo echo do you remember to depend first echo if define define ifdef subdirs tiff endif define subdirs dependsubdirs dubdirs endif echo echo do you remember to depend first echo echo build xv echo subdirs tiff endif define define subdirs endif echo build xv echo srcs xv deplibs bggen deplibs srcs ifdef dependsubdirs subdirs endif xv deplibs bggen deplibs if define sun define sysv define sighold define sigrelse define else define sigblock define sigblock define endif for most folk define define endif if define sun define sysv define define x x endif include file include if define sun define sysv include else include endif include include if rm cc o rm else rm cc o rm endif depend rm george lindholm phone university compute service ubc internet lindholm bitnet usergnl ubcmtsg 