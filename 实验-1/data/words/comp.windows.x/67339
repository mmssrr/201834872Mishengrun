from subject re color will client like it in roeber frederick roeber write i write x server for some equipment the be truecolor in yuv in x term it color i would like to the server just present this but i not sure if this will be to most x client the three problem i see the though huge be pixel would be byte wide the life in yuv the rgb yuv will introduce some round error we try to ship x server once only support truecolor the problem we encounter be client write with logic like the follow if depth be these client need deep in the root window to run correctly other client do even bother to do much just outright they with xstorecolors doe work on with or truecolor but client do bother to check they just throw xstorecolor you though both client error this be the with so client it for you to expose pseudocolor root window it ever would be for these client writer to their client to do the right thing 