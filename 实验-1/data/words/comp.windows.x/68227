from mlt turok subject problem hi netters we problem with it doe know how to correctly process if in c preprocessor it everything to true no the for choke on one of include file distribute by sun if define define stdc include define index strchr define rindex strrchr else bsd stdc include endif stdc here both if correspond else to try to open the file which doe exist under we lot of if in some of our code we get build off the net it do fix the problem in it should be note sun distribution mention this problem bug if you do not the source for cpp the berkeley c preprocessor then will be compile in such if directive will to true of their this the wrong include directive to be should simply it own write for if expression come up with some to other will to it with u since we often experience in our newsfeed reply directly to my i post in turok resource inc mlt fifth voice 