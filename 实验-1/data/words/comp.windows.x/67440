from kelsey joe kelsey subject re xv in lindholm george lindholm write in lindholm george lindholm write in john write here be the first the follow fix some problem with the file it possible to compile under without to use the ucb stuff oops i do get the process right i get from do the work on here be the fix the for need to use ifdef the overly complex if define sun define sysv in order to compile xv on do work very well use to get better result otherwise compile just fine on my esix system v box with i just uncommented the gcc definition in the to the copt line modify libs to include lsocket lnsl john if you just use the thing like this do even to get mention ifdef define sighold define sigrelse define else define sigblock define sigblock define endif for most folk define define endif ifdef define define endif 