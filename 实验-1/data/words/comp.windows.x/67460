from dlemoin d lemoine subject problem i on one the on both hp model the be use with the be correctly except the color wrong the server on the other be use different i try the pixel rgb on the perform lookup the new this do work some rgb combo do exist in the new be there to force the server to color into set pixel or be there simpler to solve this problem i try use but could get to work either help would be don lemoine center division dlemoin 