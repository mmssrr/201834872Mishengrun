from subject re drive security for fbi do encryption of your keyrings put the result with somewhere they wo be notice eg in the window directory nobody know those file do encryption of file will decrypt the keyrings to will set to point it set up file to decrypt execute the first on disk it the of the file so there be no link between this file the non-existent file refer to by the rest of the system comment it so it look like test script for fool with pgp set to the pgp directory the keyrings with pgp in the directory with pgp good you with but serious use of pgp set of key with your simple never use it or use it your widely publish key for low-security when need run the second file sure on when you the concussion hit the power switch cheer put log nc freenet on the fire begin pgp public key block version hlnv end pgp public key block 