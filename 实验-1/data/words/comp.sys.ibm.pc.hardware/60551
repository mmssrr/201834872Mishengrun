from subject dec mt scsi conflict i dec nt scsi controller disk cd-rom drive when i ethernet reboot the system i receive error boot device not be find pull the reboot everything be fine i move the controller to slot different position slot before the controller slot the controller with the result dec respond to the problem yet help would be t director university computer science phone prospect street room new ct 