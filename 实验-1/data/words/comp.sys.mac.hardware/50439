from lex lex sonderen subject re cd-rom multisession in efb write i i know multisession cd-rom be if you do more photo but if it be just cd-rom drive do second session do you need one for too if you write second time to cd you need to multi-session to the second session whether it be photo or just or here be simplify of look it the first session the directory structure burn some the second session newer directory structure but the first directory still exist you not only so if you put in drive it will only look to the first directory think it find multisession kindof the drive will look for the directory write to the disc older system look only for directory not multiple directory into lex sonderen philip 