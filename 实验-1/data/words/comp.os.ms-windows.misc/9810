from bkph berthold horn subject re in down down write so good there be diff whether or not be turn on or not be it worth it to run with these better printer technology tt there some font only p font if you p font you to use use otherwise it be of system resource or if you need to use service they only set up to use type font from i pure the result of output tt font type be not good use type font in the first unless you print to file with the correct resolution set for the output device setter problem with tt font in window be they do get convert to ok the hint be even use while this be do but the result font not hint the result be they will work fine on device of the resolution by the printer driver when the p file be but they will not look good when print different resolution so when you print to p printer with the p driver set up for this printer result be quite good but if you p file for printer run it on device of different resolution such setter then the result not be so good 